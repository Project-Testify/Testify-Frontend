import { Typography } from "antd";
const { Title} = Typography;

export const CandidateActivityHistory = () => {
    return (
        <div style={{ padding: '24px' }}>
          <Typography>
            <Title level={2}>Activity History</Title>
          </Typography>
        </div>
      );
};