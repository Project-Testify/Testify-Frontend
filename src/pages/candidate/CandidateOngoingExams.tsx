import { Typography } from "antd";
const { Title} = Typography;

export const CandidateOngoingExams = () => {
    return (
        <div style={{ padding: '24px' }}>
          <Typography>
            <Title level={2}>Ongoing Exams</Title>
          </Typography>
        </div>
      );
};