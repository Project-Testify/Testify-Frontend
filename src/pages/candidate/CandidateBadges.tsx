import { Typography } from "antd";
const { Title} = Typography;

export const CandidateBadges = () => {
    return (
        <div style={{ padding: '24px' }}>
          <Typography>
            <Title level={2}>Candidate Badges</Title>
          </Typography>
        </div>
      );
};