import { Typography } from "antd";
const { Title} = Typography;

export const CandidateUpcomingExams = () => {
    return (
        <div style={{ padding: '24px' }}>
          <Typography>
            <Title level={2}>Upcoming Exams</Title>
          </Typography>
        </div>
      );
};