import { Typography } from "antd";
const { Title} = Typography;

export const CandidateCompletedExams = () => {
    return (
        <div style={{ padding: '24px' }}>
          <Typography>
            <Title level={2}>Completed Exams</Title>
          </Typography>
        </div>
      );
};