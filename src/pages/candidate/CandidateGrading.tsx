import { Typography } from "antd";
const { Title} = Typography;

export const CandidateGrading = () => {
    return (
        <div style={{ padding: '24px' }}>
          <Typography>
            <Title level={2}>Candidate Grading</Title>
          </Typography>
        </div>
      );
};